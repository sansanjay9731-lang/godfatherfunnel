import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free AI Visibility Audit — Check Your Score | Godfather Funnel AI",
  description:
    "Enter your website URL and get your AI visibility score in 30 seconds. See if ChatGPT, Google AI, and Perplexity can find your business. Free, instant, no signup required.",
};

export default function AuditLayout({ children }: { children: React.ReactNode }) {
  return children;
}
